# Gall_species actually turns to Gall_species.V1 (..?)
x <- x[, .(Gall_sp = Gall_species, Parasitoids = V1)]
x <- x[-1]
# so change names back
setnames(x = x, old = "Gall_sp.V1", "Gall_sp")
# next we want to split on forward slashes and take the first element
x[, Parasitoid := gsub(" /.*", "", Parasitoids)]
# create new column for number if recorded, then remove next bracket, then turn to numeric
x[, Total_number := gsub(".* \\(", "", Parasitoid)][, Total_number := gsub("\\)", "", Total_number)][, Total_number := as.numeric(Total_number)]
# clean up the parasitoid column
x[, Parasitoid := gsub(" \\(.*", "", Parasitoid)]
# get status/generation of gall wasp out
x[, Status := gsub(".*\\(", "", Gall_sp)][, Status := gsub("\\)", "", Status)]
# lastly, get the countries out, and numbers separately
#x[, Per_country := stri_extract_all(str = Parasitoids, regex = countries)]
# split on forward slash but remove first element
x[, Per_country := strsplit(x = Parasitoids, split = "/", fixed = TRUE)]
# remove authority and after...
x[, Gall_sp := gsub(" [[:upper:]].*| \\(.*", "", Gall_sp)]
})
# Per_country is a list of vectors and we want only country + number in a list
# to clean use lapply and regex
# list 113 has a duplicate column for some reason.
# m[[113]] <- m[[113]][, -"Gall_sp.Gall_sp"]
m2 <- rbindlist(m)
n <- lapply(m2$Per_country, function(x){
# remove first element
y <- x[-1]
# remove references
y2 <- gsub("\\([a-zA-Z].*", "", y)
# remove brackets
y3 <- gsub("\\(", "", y2)
y4 <- gsub("\\)", "", y3)
# replace comma
y5 <- gsub(",", "", y4)
# replace asterisk
y6 <- gsub("\\*", "", y5)
y6
}
)
# now remove guff if there was no number (ü + /)
#z <- gsub(" /.*", "", y)
#z <- gsub(" Q\\..*", "", z)
#z <- gsub(" ü.*", "", z)
# remove asterisks and space at end of string
#z <- gsub("\\*", "", z)
#z <- gsub(" $", "", z)
#z
#})
# replace per country with n
m2[, Per_country := sapply(n, function(x) paste(x, collapse = ","))]
# rearrange and clean
m3 <- m2[, .(Gall_sp, Status, Parasitoid, Total_number, Per_country)]
# split the per country column
m3[, Per_country := strsplit(Per_country, ",")]
m3[grepl(pattern = "\\\\", Per_country)]
# I need this list column to be cast so each element of the list is returned as a separate row
# could either take each element out as a separate column (inefficient?)
# or label each record with unique ID and merge later
m3[, ID := 1:nrow(m3)]
# group by unique ID                                       then! merge back with data
m4 <- m3[,.(Per_country = unlist(Per_country)), by = .(ID)][m3[,-c("Per_country", "Total_number")], on=.(ID)]
# create the Total Number again (per country number)
m4[, Number := stringi::stri_extract_first_regex(str = m4$Per_country, pattern = "[[:digit:]]+")]
# fix Per_country & names
m4[, Per_country := gsub("^ ", "", Per_country)]
m4[, Country := gsub(" .*", "", m4$Per_country)]
# get Quercus out, delete everything before first Q
m4[, Tree_host := unlist(stringi::stri_extract_all(regex = "Q\\..*", str = Per_country))]
# remove per country
m4 <- m4[,-"Per_country"]
m4
Country <- "Andorra, Austria, Azerbaijan, Belgium, Bulgaria, Switzerland, Czech Republic, Germany, Denmark, Algeria, Spain, Finland, France, United Kingdom, Greece, Croatia, Hungary, Ireland,Israel, Italy, Jordan, Lebanon, Morocco, Moldova, Netherlands, Poland, Portugal, Romania, Serbia, Russian Federation, Sweden, Slovenia, Slovakia, Tunisia, Turkey, Ukraine, Yugoslavia"
Per_country <- c("AD, AT, AZ, BE, BG, CH, CZ, DE, DK, DZ, ES, FI, FR, GB, GR, HR, HU, IE, IS, IT, JO, LB, MA, MD, NL, PL, PT, RO, RS, RU, SE, SI, SK, TN, TR, UA, YU")
country_names <- data.table(Country = sapply(Per_country, function(x) strsplit(x, ","))[[1]],
Country2 = sapply(Country, function(x) strsplit(x, ","))[[1]])
country_names[,Country := gsub("^ ", "", Country)]
# final fix
m5 <- country_names[m4, on = .(Country)]#[,-c("Per_country", "ID")]
m5
fwrite(m5, "./MB_KM_galls080320.csv")
library(data.table)
library(stringi)
test <- fread(text = "./2013AskewZootaxa_KM_290120.txt", sep = "\n", header = FALSE)
# I think got to create a factor determining whether it is a gall wasp or not.
# this is the genus
test[, V2 := grepl(pattern = "^[[:upper:]]{1}[[:lower:]]* [[:upper:]]{1}[[:lower:]]*", V1)]
# there are 14 genera
table(test$V2)
# remove genera
test <- test[!test$V2,][,.(V1)]
# match rows on a regex for species of gall wasp. Upper, lower, space, lower, upper (or parenthesis and capital)
vec <- grep(pattern = "^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* [[:upper:]]{1}|^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* \\([[:upper:]]|^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* \\([[:lower:]]", x = test$V1)
length(vec) # 114 gall wasp species?
# split the list on gall wasp species
lis <- list()
for(i in 1:156){
j <- i+1
lis[[i]] <- test[vec[i:j][1]:(vec[i:j][2]-1)]
}
lis[156]
# what are all the countries?
countries <- c("AD, AT, AZ, BE, BG, CH, CZ, DE, DK, DZ, ES, FI, FR, GB, GR, HR, HU, IE, IS, IT, JO, LB, MA, MD, NL, PL, PT, RO, RS, RU, SE, SI, SK, TN, TR, UA, YU")
countries <- gsub(", ", "|", countries)
countries <- stringi::stri_replace_all_regex(str = countries, pattern = "\\|", replacement = ").{8}|(?=")
countries <- paste0("(?=", countries, ").{8}")
m<-lapply(lis, function(x){
# x refers to the data table
# this creates the gall species and parasitoids along with metadata
y <- dim(x)[1]
Gall_species <- x[1]
# Gall_species actually turns to Gall_species.V1 (..?)
x <- x[, .(Gall_sp = Gall_species, Parasitoids = V1)]
x <- x[-1]
# so change names back
setnames(x = x, old = "Gall_sp.V1", "Gall_sp")
# next we want to split on forward slashes and take the first element
x[, Parasitoid := gsub(" /.*", "", Parasitoids)]
# create new column for number if recorded, then remove next bracket, then turn to numeric
x[, Total_number := gsub(".* \\(", "", Parasitoid)][, Total_number := gsub("\\)", "", Total_number)][, Total_number := as.numeric(Total_number)]
# clean up the parasitoid column
x[, Parasitoid := gsub(" \\(.*", "", Parasitoid)]
# get status/generation of gall wasp out
x[, Status := gsub(".*\\(", "", Gall_sp)][, Status := gsub("\\)", "", Status)]
# lastly, get the countries out, and numbers separately
#x[, Per_country := stri_extract_all(str = Parasitoids, regex = countries)]
# split on forward slash but remove first element
x[, Per_country := strsplit(x = Parasitoids, split = "/", fixed = TRUE)]
# remove authority and after...
x[, Gall_sp := gsub(" [[:upper:]].*| \\(.*", "", Gall_sp)]
})
# Per_country is a list of vectors and we want only country + number in a list
# to clean use lapply and regex
# list 113 has a duplicate column for some reason.
# m[[113]] <- m[[113]][, -"Gall_sp.Gall_sp"]
m2 <- rbindlist(m)
n <- lapply(m2$Per_country, function(x){
# remove first element
y <- x[-1]
# remove references
y2 <- gsub("\\([a-zA-Z].*", "", y)
# remove brackets
y3 <- gsub("\\(", "", y2)
y4 <- gsub("\\)", "", y3)
# replace comma
y5 <- gsub(",", "", y4)
# replace asterisk
y6 <- gsub("\\*", "", y5)
y6
}
)
# now remove guff if there was no number (ü + /)
#z <- gsub(" /.*", "", y)
#z <- gsub(" Q\\..*", "", z)
#z <- gsub(" ü.*", "", z)
# remove asterisks and space at end of string
#z <- gsub("\\*", "", z)
#z <- gsub(" $", "", z)
#z
#})
# replace per country with n
m2[, Per_country := sapply(n, function(x) paste(x, collapse = ","))]
# rearrange and clean
m3 <- m2[, .(Gall_sp, Status, Parasitoid, Total_number, Per_country)]
# split the per country column
m3[, Per_country := strsplit(Per_country, ",")]
m3[grepl(pattern = "\\\\", Per_country)]
# I need this list column to be cast so each element of the list is returned as a separate row
# could either take each element out as a separate column (inefficient?)
# or label each record with unique ID and merge later
m3[, ID := 1:nrow(m3)]
# group by unique ID                                       then! merge back with data
m4 <- m3[,.(Per_country = unlist(Per_country)), by = .(ID)][m3[,-c("Per_country", "Total_number")], on=.(ID)]
# create the Total Number again (per country number)
m4[, Number := stringi::stri_extract_first_regex(str = m4$Per_country, pattern = "[[:digit:]]+")]
# fix Per_country & names
m4[, Per_country := gsub("^ ", "", Per_country)]
m4[, Country := gsub(" .*", "", m4$Per_country)]
# get Quercus out, delete everything before first Q
m4[, Tree_host := unlist(stringi::stri_extract_all(regex = "Q\\..*", str = Per_country))]
# remove per country
m4 <- m4[,-"Per_country"]
Country <- "Andorra, Austria, Azerbaijan, Belgium, Bulgaria, Switzerland, Czech Republic, Germany, Denmark, Algeria, Spain, Finland, France, United Kingdom, Greece, Croatia, Hungary, Ireland,Israel, Italy, Jordan, Lebanon, Morocco, Moldova, Netherlands, Poland, Portugal, Romania, Serbia, Russian Federation, Sweden, Slovenia, Slovakia, Tunisia, Turkey, Ukraine, Yugoslavia"
Per_country <- c("AD, AT, AZ, BE, BG, CH, CZ, DE, DK, DZ, ES, FI, FR, GB, GR, HR, HU, IE, IS, IT, JO, LB, MA, MD, NL, PL, PT, RO, RS, RU, SE, SI, SK, TN, TR, UA, YU")
country_names <- data.table(Country = sapply(Per_country, function(x) strsplit(x, ","))[[1]],
Country2 = sapply(Country, function(x) strsplit(x, ","))[[1]])
country_names[,Country := gsub("^ ", "", Country)]
# final fix
m5 <- country_names[m4, on = .(Country)]#[,-c("Per_country", "ID")]
m5
fwrite(m5, "./MB_KM_galls080320_2.csv")
# this document has be iteratively updated based on errors.
test <- fread(text = "./2013AskewZootaxa_KM_290120.txt", sep = "\n", header = FALSE)
# I think got to create a factor determining whether it is a gall wasp or not.
# this is the genus
test[, V2 := grepl(pattern = "^[[:upper:]]{1}[[:lower:]]* [[:upper:]]{1}[[:lower:]]*", V1)]
# there are 14 genera
table(test$V2)
# remove genera
test <- test[!test$V2,][,.(V1)]
# match rows on a regex for species of gall wasp. Upper, lower, space, lower, upper (or parenthesis and capital)
vec <- grep(pattern = "^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* [[:upper:]]{1}|^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* \\([[:upper:]]|^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* \\([[:lower:]]", x = test$V1)
length(vec) # 114 gall wasp species?
# split the list on gall wasp species
lis <- list()
for(i in 1:156){
j <- i+1
lis[[i]] <- test[vec[i:j][1]:(vec[i:j][2]-1)]
}
for(i in 1:155){
j <- i+1
lis[[i]] <- test[vec[i:j][1]:(vec[i:j][2]-1)]
}
lis[155]
# what are all the countries?
countries <- c("AD, AT, AZ, BE, BG, CH, CZ, DE, DK, DZ, ES, FI, FR, GB, GR, HR, HU, IE, IS, IT, JO, LB, MA, MD, NL, PL, PT, RO, RS, RU, SE, SI, SK, TN, TR, UA, YU")
countries <- gsub(", ", "|", countries)
countries <- stringi::stri_replace_all_regex(str = countries, pattern = "\\|", replacement = ").{8}|(?=")
countries <- paste0("(?=", countries, ").{8}")
m<-lapply(lis, function(x){
# x refers to the data table
# this creates the gall species and parasitoids along with metadata
y <- dim(x)[1]
Gall_species <- x[1]
# Gall_species actually turns to Gall_species.V1 (..?)
x <- x[, .(Gall_sp = Gall_species, Parasitoids = V1)]
x <- x[-1]
# so change names back
setnames(x = x, old = "Gall_sp.V1", "Gall_sp")
# next we want to split on forward slashes and take the first element
x[, Parasitoid := gsub(" /.*", "", Parasitoids)]
# create new column for number if recorded, then remove next bracket, then turn to numeric
x[, Total_number := gsub(".* \\(", "", Parasitoid)][, Total_number := gsub("\\)", "", Total_number)][, Total_number := as.numeric(Total_number)]
# clean up the parasitoid column
x[, Parasitoid := gsub(" \\(.*", "", Parasitoid)]
# get status/generation of gall wasp out
x[, Status := gsub(".*\\(", "", Gall_sp)][, Status := gsub("\\)", "", Status)]
# lastly, get the countries out, and numbers separately
#x[, Per_country := stri_extract_all(str = Parasitoids, regex = countries)]
# split on forward slash but remove first element
x[, Per_country := strsplit(x = Parasitoids, split = "/", fixed = TRUE)]
# remove authority and after...
x[, Gall_sp := gsub(" [[:upper:]].*| \\(.*", "", Gall_sp)]
})
# Per_country is a list of vectors and we want only country + number in a list
# to clean use lapply and regex
# list 113 has a duplicate column for some reason.
# m[[113]] <- m[[113]][, -"Gall_sp.Gall_sp"]
m2 <- rbindlist(m)
n <- lapply(m2$Per_country, function(x){
# remove first element
y <- x[-1]
# remove references
y2 <- gsub("\\([a-zA-Z].*", "", y)
# remove brackets
y3 <- gsub("\\(", "", y2)
y4 <- gsub("\\)", "", y3)
# replace comma
y5 <- gsub(",", "", y4)
# replace asterisk
y6 <- gsub("\\*", "", y5)
y6
}
)
# now remove guff if there was no number (ü + /)
#z <- gsub(" /.*", "", y)
#z <- gsub(" Q\\..*", "", z)
#z <- gsub(" ü.*", "", z)
# remove asterisks and space at end of string
#z <- gsub("\\*", "", z)
#z <- gsub(" $", "", z)
#z
#})
# replace per country with n
m2[, Per_country := sapply(n, function(x) paste(x, collapse = ","))]
# rearrange and clean
m3 <- m2[, .(Gall_sp, Status, Parasitoid, Total_number, Per_country)]
# split the per country column
m3[, Per_country := strsplit(Per_country, ",")]
m3[grepl(pattern = "\\\\", Per_country)]
# I need this list column to be cast so each element of the list is returned as a separate row
# could either take each element out as a separate column (inefficient?)
# or label each record with unique ID and merge later
m3[, ID := 1:nrow(m3)]
# group by unique ID                                       then! merge back with data
m4 <- m3[,.(Per_country = unlist(Per_country)), by = .(ID)][m3[,-c("Per_country", "Total_number")], on=.(ID)]
# create the Total Number again (per country number)
m4[, Number := stringi::stri_extract_first_regex(str = m4$Per_country, pattern = "[[:digit:]]+")]
# fix Per_country & names
m4[, Per_country := gsub("^ ", "", Per_country)]
m4[, Country := gsub(" .*", "", m4$Per_country)]
# get Quercus out, delete everything before first Q
m4[, Tree_host := unlist(stringi::stri_extract_all(regex = "Q\\..*", str = Per_country))]
# remove per country
m4 <- m4[,-"Per_country"]
Country <- "Andorra, Austria, Azerbaijan, Belgium, Bulgaria, Switzerland, Czech Republic, Germany, Denmark, Algeria, Spain, Finland, France, United Kingdom, Greece, Croatia, Hungary, Ireland,Israel, Italy, Jordan, Lebanon, Morocco, Moldova, Netherlands, Poland, Portugal, Romania, Serbia, Russian Federation, Sweden, Slovenia, Slovakia, Tunisia, Turkey, Ukraine, Yugoslavia"
Per_country <- c("AD, AT, AZ, BE, BG, CH, CZ, DE, DK, DZ, ES, FI, FR, GB, GR, HR, HU, IE, IS, IT, JO, LB, MA, MD, NL, PL, PT, RO, RS, RU, SE, SI, SK, TN, TR, UA, YU")
country_names <- data.table(Country = sapply(Per_country, function(x) strsplit(x, ","))[[1]],
Country2 = sapply(Country, function(x) strsplit(x, ","))[[1]])
country_names[,Country := gsub("^ ", "", Country)]
# final fix
m5 <- country_names[m4, on = .(Country)]#[,-c("Per_country", "ID")]
fwrite(m5, "./MB_KM_galls080320_2.csv")
fwrite(m5, "./MB_KM_galls080320_3.csv")
m5
fwrite(m5, "./MB_KM_galls080320_3.csv")
library(data.table)
library(stringi)
# this document has be iteratively updated based on errors.
test <- fread(text = "./2013AskewZootaxa_KM_290120.txt", sep = "\n", header = FALSE)
# I think got to create a factor determining whether it is a gall wasp or not.
# this is the genus
test[, V2 := grepl(pattern = "^[[:upper:]]{1}[[:lower:]]* [[:upper:]]{1}[[:lower:]]*", V1)]
# there are 14 genera
table(test$V2)
# remove genera
test <- test[!test$V2,][,.(V1)]
# match rows on a regex for species of gall wasp. Upper, lower, space, lower, upper (or parenthesis and capital)
vec <- grep(pattern = "^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* [[:upper:]]{1}|^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* \\([[:upper:]]|^[[:upper:]]{1}[[:lower:]]* [[:lower:]]* \\([[:lower:]]", x = test$V1)
length(vec) # 114 gall wasp species?
# split the list on gall wasp species
lis <- list()
for(i in 1:155){
j <- i+1
lis[[i]] <- test[vec[i:j][1]:(vec[i:j][2]-1)]
}
lis[155]
lis[156]
# what are all the countries?
countries <- c("AD, AT, AZ, BE, BG, CH, CZ, DE, DK, DZ, ES, FI, FR, GB, GR, HR, HU, IE, IS, IT, JO, LB, MA, MD, NL, PL, PT, RO, RS, RU, SE, SI, SK, TN, TR, UA, YU")
countries <- gsub(", ", "|", countries)
countries <- stringi::stri_replace_all_regex(str = countries, pattern = "\\|", replacement = ").{8}|(?=")
countries <- paste0("(?=", countries, ").{8}")
m<-lapply(lis, function(x){
# x refers to the data table
# this creates the gall species and parasitoids along with metadata
y <- dim(x)[1]
Gall_species <- x[1]
# Gall_species actually turns to Gall_species.V1 (..?)
x <- x[, .(Gall_sp = Gall_species, Parasitoids = V1)]
x <- x[-1]
# so change names back
setnames(x = x, old = "Gall_sp.V1", "Gall_sp")
# next we want to split on forward slashes and take the first element
x[, Parasitoid := gsub(" /.*", "", Parasitoids)]
# create new column for number if recorded, then remove next bracket, then turn to numeric
x[, Total_number := gsub(".* \\(", "", Parasitoid)][, Total_number := gsub("\\)", "", Total_number)][, Total_number := as.numeric(Total_number)]
# clean up the parasitoid column
x[, Parasitoid := gsub(" \\(.*", "", Parasitoid)]
# get status/generation of gall wasp out
x[, Status := gsub(".*\\(", "", Gall_sp)][, Status := gsub("\\)", "", Status)]
# lastly, get the countries out, and numbers separately
#x[, Per_country := stri_extract_all(str = Parasitoids, regex = countries)]
# split on forward slash but remove first element
x[, Per_country := strsplit(x = Parasitoids, split = "/", fixed = TRUE)]
# remove authority and after...
x[, Gall_sp := gsub(" [[:upper:]].*| \\(.*", "", Gall_sp)]
})
# Per_country is a list of vectors and we want only country + number in a list
# to clean use lapply and regex
# list 113 has a duplicate column for some reason.
# m[[113]] <- m[[113]][, -"Gall_sp.Gall_sp"]
m2 <- rbindlist(m)
n <- lapply(m2$Per_country, function(x){
# remove first element
y <- x[-1]
# remove references
y2 <- gsub("\\([a-zA-Z].*", "", y)
# remove brackets
y3 <- gsub("\\(", "", y2)
y4 <- gsub("\\)", "", y3)
# replace comma
y5 <- gsub(",", "", y4)
# replace asterisk
y6 <- gsub("\\*", "", y5)
y6
}
)
# now remove guff if there was no number (ü + /)
#z <- gsub(" /.*", "", y)
#z <- gsub(" Q\\..*", "", z)
#z <- gsub(" ü.*", "", z)
# remove asterisks and space at end of string
#z <- gsub("\\*", "", z)
#z <- gsub(" $", "", z)
#z
#})
# replace per country with n
m2[, Per_country := sapply(n, function(x) paste(x, collapse = ","))]
# rearrange and clean
m3 <- m2[, .(Gall_sp, Status, Parasitoid, Total_number, Per_country)]
# split the per country column
m3[, Per_country := strsplit(Per_country, ",")]
m3[grepl(pattern = "\\\\", Per_country)]
# I need this list column to be cast so each element of the list is returned as a separate row
# could either take each element out as a separate column (inefficient?)
# or label each record with unique ID and merge later
m3[, ID := 1:nrow(m3)]
# group by unique ID                                       then! merge back with data
m4 <- m3[,.(Per_country = unlist(Per_country)), by = .(ID)][m3[,-c("Per_country", "Total_number")], on=.(ID)]
# create the Total Number again (per country number)
m4[, Number := stringi::stri_extract_first_regex(str = m4$Per_country, pattern = "[[:digit:]]+")]
# fix Per_country & names
m4[, Per_country := gsub("^ ", "", Per_country)]
m4[, Country := gsub(" .*", "", m4$Per_country)]
# get Quercus out, delete everything before first Q
m4[, Tree_host := unlist(stringi::stri_extract_all(regex = "Q\\..*", str = Per_country))]
# remove per country
m4 <- m4[,-"Per_country"]
Country <- "Andorra, Austria, Azerbaijan, Belgium, Bulgaria, Switzerland, Czech Republic, Germany, Denmark, Algeria, Spain, Finland, France, United Kingdom, Greece, Croatia, Hungary, Ireland,Israel, Italy, Jordan, Lebanon, Morocco, Moldova, Netherlands, Poland, Portugal, Romania, Serbia, Russian Federation, Sweden, Slovenia, Slovakia, Tunisia, Turkey, Ukraine, Yugoslavia"
Per_country <- c("AD, AT, AZ, BE, BG, CH, CZ, DE, DK, DZ, ES, FI, FR, GB, GR, HR, HU, IE, IS, IT, JO, LB, MA, MD, NL, PL, PT, RO, RS, RU, SE, SI, SK, TN, TR, UA, YU")
country_names <- data.table(Country = sapply(Per_country, function(x) strsplit(x, ","))[[1]],
Country2 = sapply(Country, function(x) strsplit(x, ","))[[1]])
country_names[,Country := gsub("^ ", "", Country)]
# final fix
m5 <- country_names[m4, on = .(Country)]#[,-c("Per_country", "ID")]
m5
fwrite(m5, "./MB_KM_galls080320_4.csv")
library(ggplot2)
fread
askew <- fread("./MB_KM_galls080320_4.csv")
askew
askew[!is.na(Number)]
askew[, .(sum(Number)), by = .(Gall_sp, Parasitoid)]
askew[, .(sum(Number)), by = .(Gall_sp, Parasitoid)][!is.na(V1)]
askew[, .(sum(Number)), by = .(Gall_sp, Parasitoid)][!is.na(V1)][1:1000]
askew[, .(sum(Number)), by = .(Gall_sp, Parasitoid)][!is.na(V1)][1:100]
askew[, .(sum(Number)), by = .(Gall_sp, Parasitoid)][!is.na(V1)][order(-V1)]
askew[, .(sum(Number)), by = .(Gall_sp, Parasitoid)][!is.na(V1)][order(-V1)][1:100]
askew[, unique(Gall_sp)]
askew[, .(unique(Gall_sp))]
askew[.(sum(Number)), by=.(Parasitoid)][!is.na(V1)][order(-V1)]
askew[,.(sum(Number)), by=.(Parasitoid)][!is.na(V1)][order(-V1)]
askew[,.(sum(Number)), by=.(Parasitoid)][!is.na(V1)][order(-V1)][1:100]
askew[,.(sum(Number)), by=.(Parasitoid)]
askew[,.(sum(Number)), by=.(Parasitoid)][order(-V1)]
askew[, .(sum(Number)), by = .(Gall_sp, Parasitoid)][!is.na(V1)][order(-V1)][1:100]
askew[!is.na(Number)][,.(sum(Number)), by=.(Parasitoid)][order(-V1)]
askew[!is.na(Number)][,.(sum(Number)), by=.(Parasitoid)][order(-V1)][1:100]
askew[Parasitoid == "Synergus umbraculus"]
askew[Parasitoid == "Synergus umbraculus"][order(-Number)]
askew[Parasitoid == "Synergus umbraculus"][order(-Number)][1:100]
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)]
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100]
library(dplyr)
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(aes(x = Gall_sp, y = Total))+
geom_col
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(aes(x = Gall_sp, y = Total))+
geom_col()
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(aes(x = reorder(Gall_sp, Total), y = Total))+
geom_col()
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
theme(axis.text.x = element_text(angle = 60, hjust = -1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100]
c(1:5)
c(1:5) %>% mean()
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(data = ., aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
facet_wrap(~)+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(data = , aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
facet_wrap(~)+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(., aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
facet_wrap(~)+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)][1:100] %>%
ggplot(., aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp)][order(-Total)]
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp, Status)][order(-Total)]
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp, Status)][order(-Total)][1:100] %>%
ggplot(aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
facet_wrap(~Status)+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp, Status)][order(-Total)][1:100] %>%
ggplot(aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
facet_wrap(~Status %in% c("asexual", "bisexual"))+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp, Status)][order(-Total)][Status %in% c("asexual", "bisexual")] %>%
ggplot(aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
facet_wrap(~Status)+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[Gall_sp == "Biorhiza pallida" & Status == "asexual"]
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp, Status)][order(-Total)][Status %in% c("asexual", "bisexual")]
askew[!is.na(Number), .(Total = sum(Number)), .(Gall_sp, Status)][order(-Total)][Status %in% c("asexual", "bisexual")] %>%
ggplot(aes(x = reorder(Gall_sp, -Total), y = Total))+
geom_col()+
facet_wrap(~Status, scales = "free_y")+
theme(axis.text.x = element_text(angle = 60, hjust = 1))
askew[!is.na(Number), .(sum(Number)), .(Tree_host)]
askew[!is.na(Number), .(sum(Number)), .(Tree_host)][order(-V1)]
taxonlookup::lookup_table(species_list = "Q. robur")
taxonlookup::lookup_table(species_list = "Quercus robur")
